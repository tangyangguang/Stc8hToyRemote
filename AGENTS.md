# 项目规则

## IoT高效推进与验证原则

- 按当前合理方案直接实施，零历史兼容、零数据迁移；不为旧测试数据保留兼容层、迁移工具或双轨实现，不扩大到凭据和无关数据清除。
- 开发交付以代码评审及本次改动相关的快速、定向检查为准：覆盖关键成功路径、明显故障与必要安全边界；通过且无阻塞问题后立即推进，不反复规划或追加无关测试。
- 长稳、soak、长时间压测、物理满容量及反复断电实验由用户在自己搭建的半生产、试运行环境中逐步验证并反馈。代理不主动执行、不等待实验窗口，也不把这些列为编码、提交或当前开发交付的前置条件；仅在用户明确要求专项实验时执行。
- 已通过且未受影响的检查直接复用；只在新改动、明确失败或证据疑点影响当前结论时重验。不得默认扩展全量/多芯片/多Core矩阵、反复重建工具链或占用机器长时间运行；纯规则/文档修改只检查内容、链接与diff。
- 效率不等于削弱TLS、重连、WDT、OTA、CRC、写入完整性或执行器安全；已知缺陷必须修复或明确停下说明。未做的运行验证如实标注，不冒称通过。本文及所引用文档中的旧验证要求若与本节冲突，以本节为准；正式发布与物理操作仍按各自授权边界执行。

> 目录于 2026-09-08 从 `/Users/tyg/dir/codex_dir/Stc8hToyRemote` 迁移到 `/Users/tyg/workspace/iot/devices/Stc8hToyRemote`；仓库仍可能包含旧路径，执行相关命令前先按新目录核对并修正，完成本项目的路径清理和验证后删除本条。

- 本项目重写 STC8H 玩具遥控器和接收机固件。
- `legacy/` 目录只读，只作为需求和行为参考，不能修改其中代码。
- 新代码必须引用相邻基础库 `../Stc8hBase`，不复制基础库源码。
- 不兼容旧 API，不复用旧 `my_nRF24L01` 实现。
- 公共硬件能力优先使用 `Stc8hBase` 的 `core/hal/drivers/protocols`。
- nRF24L01 通信使用 `drv_nrf24l01` 和 `proto_rf_link`。
- 玩具遥控业务协议只放在 `shared/`，不要污染 `Stc8hBase`。
- 无线 payload 必须手工按字节打包，不直接发送 C struct。
- receiver 掉线后必须进入安全状态：电机停止，灯和蜂鸣器关闭或进入明确安全状态。
- 先实现最小可编译骨架，再做 nRF24 双板通信，最后迁移业务输入输出。
- 发现基础库确有缺失功能或 bug 时，及时给出给基础库项目使用的提示词，不把该提示词或基础库问题记录写入本项目文档。
- 基础库不是默认成熟稳定件；应用项目接入基础库的一项重要目的，就是验证和打磨基础库能力，不能未经实测就完全相信其稳定性。
- 涉及持久化配置时，先设计版本、校验、备份和恢复策略，再实施。
- 完成新功能或修复后，验证、提交；有远端时再推送。
