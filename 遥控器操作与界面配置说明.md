# 遥控器操作与界面配置说明

本文说明 ToyRemote 遥控器 controller 的日常操作、TM1637 四位数码管界面、连接/扫频状态和配置模式。它面向实际使用、调试和硬件核对；系统协议、PWM、receiver 输出和链路参数见 `遥控器与接收机核心逻辑说明.md`。

## 1. 文档边界

本文件回答这些问题：

- 遥控器屏幕上 `C076`、`L076`、`S076`、`F076`、`E001` 分别是什么意思。
- 正常控制界面怎样看方向、速度、刹车和链路状态。
- EC11 旋钮和中键怎样操作。
- 如何进入配置模式、如何修改配置、如何保存退出。
- `P1`、`P2`、`P3`、`P4` 每个配置项具体影响什么。

不在本文件展开的内容：

- 无线 32 字节包格式。
- nRF24 空口速率、ACK payload、重发参数。
- receiver 电机 PWM、P5.4 辅助 PWM、舵机 PWM 的具体频率和 duty 映射。
- receiver 掉线安全态和本地输出细节。

这些属于系统核心逻辑和参数说明，统一放在 `遥控器与接收机核心逻辑说明.md`。

## 2. 遥控器输入部件

| 部件 | 功能 |
| --- | --- |
| EC11 旋钮 | 正常模式调速度；配置模式调当前配置项 |
| EC11 中键 | 短按刹车并清速度；长按进入/退出配置；丢链界面双击扫频 |
| 方向键/方向开关 | 切换前进/后退 |
| 独立刹车键 | 按住刹车，但不清速度 |
| 灯键 | 控制 receiver 灯光 |
| 蜂鸣器键 | 控制 receiver 蜂鸣器 |
| Fn 键 | 请求并显示电压 |
| 转向电位器 | 控制舵机转向 |

EC11 中键和独立刹车键都会触发刹车，但语义不同：

| 刹车来源 | 行为 |
| --- | --- |
| EC11 中键短按/按下 | 刹车，并把速度清到 0 |
| 独立刹车键 | 刹车，但保留当前速度 |

## 3. 开机和连接界面

遥控器开机后会读取 EEPROM 配置，优先尝试上次保存的频道。

```text
开机
  -> 显示 Cxxx，尝试保存频道
  -> 收到合法 receiver ACK 后进入正常控制界面
  -> 连续失败后显示 Lxxx，并锁定该频道重试
```

其中 `xxx` 是频道号，固定显示为 3 位。例如频道 76 显示为 `076`。

| 显示 | 含义 | 用户动作 |
| --- | --- | --- |
| `C076` | 正在尝试连接频道 76 | 等待；如果 receiver 正常，会自动进入控制界面 |
| `L076` | 频道 76 暂时连不上 | 检查 receiver 电源/距离/频道；需要换频道时双击 EC11 中键扫频 |
| `S076` | 正在扫描频道 76 | 等待扫描继续 |
| `F076` | 已找到频道 76 | 遥控器会短暂停留并保存频道，然后进入控制界面 |
| `E001` | controller nRF24 初始化失败 | 检查遥控器 nRF24 模块、SPI、供电和焊接 |

`Lxxx` 不是立刻换频道的状态。遥控器会继续在当前频道重试，避免短时遮挡或距离问题导致误扫频。只有在 `Lxxx` 界面双击 EC11 中键，才会进入手动扫频。

## 4. 正常控制界面

正常控制界面用第 1 位显示方向或刹车，第 3-4 位显示速度，中间冒号用于提示链路状态。

| 显示元素 | 含义 |
| --- | --- |
| 第 1 位 `^` | 前进 |
| 第 1 位 `v` | 后退 |
| 第 1 位 `-` | 当前处于刹车，或速度已被 EC11 中键清零 |
| 第 3-4 位 `00..99` | 当前速度 |
| 第 3-4 位 `A0` | 当前速度为 100 |
| 冒号灭 | 最近通信正常 |
| 冒号快闪 | 短时发送失败、ACK 缺失或链路不稳定 |

示例：

| 示例显示 | 含义 |
| --- | --- |
| `^ 25` | 前进，速度 25 |
| `v 80` | 后退，速度 80 |
| `^ A0` | 前进，速度 100 |
| `- 00` | 刹车且速度为 0 |
| `^:25` | 前进速度 25，但链路近期有失败或警告 |

实际数码管是四位显示，上表中的空格表示对应位为空白。

## 5. 正常模式操作

### 5.1 调速度

旋转 EC11 调整速度：

| 操作 | 效果 |
| --- | --- |
| 慢速旋转 | 每格速度变化 1 |
| 快速旋转 | 每格速度变化 5 |
| 到达下限 | 保持 0 |
| 到达上限 | 保持 100 |

速度是逻辑值 `0..100`。receiver 会把它转换成电机 PWM 输出。

### 5.2 前进和后退

方向键/方向开关直接影响前进/后退显示和发送值：

| 显示 | 方向 |
| --- | --- |
| `^` | 前进 |
| `v` | 后退 |

如果配置项 `P1` 开启了方向反向，实际发送给 receiver 的方向会反过来。这个功能用于适配方向开关接线或电机方向和预期相反的情况。

### 5.3 刹车

| 操作 | 效果 |
| --- | --- |
| 按 EC11 中键 | 刹车并清速度 |
| 按独立刹车键 | 刹车但不清速度 |
| 松开独立刹车键 | 恢复用原速度继续控制 |

刹车时 receiver 的电机输出不是安全态停止，而是刹车输出。安全态输出逻辑见核心逻辑说明。

### 5.4 灯和蜂鸣器

| 操作 | 效果 |
| --- | --- |
| 按灯键 | receiver 灯打开 |
| 松开灯键 | receiver 灯关闭 |
| 按蜂鸣器键 | receiver 蜂鸣器打开 |
| 松开蜂鸣器键 | receiver 蜂鸣器关闭 |

灯和蜂鸣器是按键状态型控制，不是固件里的翻转锁存开关。

### 5.5 电压显示

按住 Fn 键时，遥控器进入电压显示：

| 显示方式 | 含义 |
| --- | --- |
| 四位数字 | 电压，单位为百分之一伏 |
| 冒号亮 | 显示遥控器本机电压 |
| 冒号灭 | 显示 receiver 回传电压 |

示例：

| 示例显示 | 含义 |
| --- | --- |
| `07:42` | 遥控器本机电压约 7.42V |
| `0742` | receiver 回传电压约 7.42V |

Fn 松开后，显示回到正常控制界面。receiver 电压只有在 Fn 请求期间才会低频采样并回传，所以刚按下 Fn 时可能短暂显示上一次回传值。

## 6. 丢链和扫频操作

当显示 `Lxxx` 时，表示遥控器在当前频道上连续没有收到合法 receiver ACK。此时遥控器仍会锁定该频道重试。

手动扫频步骤：

```text
显示 Lxxx
  -> 双击 EC11 中键
  -> 显示 Sxxx，开始扫描
  -> 找到 receiver 后显示 Fxxx
  -> 保存找到的频道
  -> 返回正常控制界面
```

扫频顺序：

1. 先扫描 16 个预设频道：

```text
76, 72, 68, 64, 60, 56, 52, 48,
44, 40, 36, 32, 28, 24, 20, 16
```

2. 再扫描 `0..125` 全部频道。
3. 如果仍找不到，重复以上完整流程。

双击 EC11 中键只有在 `Lxxx` 丢链界面才触发扫频；在正常控制界面双击会被忽略。

## 7. 配置模式入口

在非配置状态下，长按 EC11 中键约 5 秒进入配置模式。进入后显示第一项配置 `P1`。

建议在车辆静止、轮子离地或安全环境下进入配置模式。配置模式中遥控器仍会发包，但控制内容保持停止/刹车：

```text
speed = 0
brake = 1
light = 0
buzzer = 0
aux_pwm = 0
request_voltage = 0
```

这可以避免配置过程中车辆继续按原速度运行。

## 8. 配置模式操作

配置模式下只使用 EC11：

| 操作 | 效果 |
| --- | --- |
| 旋转 EC11 | 修改当前配置项 |
| 短按 EC11 中键 | 切换到下一项配置 |
| 长按 EC11 中键约 3 秒 | 保存配置到 EEPROM 并退出 |
| 断电且未长按保存 | 放弃未保存草稿 |

配置项循环顺序：

```text
P1 -> P2 -> P3 -> P4 -> P1 ...
```

配置模式中 EC11 快转加速关闭，每个有效 detent 只改变 1 个单位，避免配置值跳变。

配置显示格式：

```text
P项:值
```

示例：

| 显示 | 含义 |
| --- | --- |
| `P1: 0` | 当前配置项为 P1，值为 0 |
| `P2: 1` | 当前配置项为 P2，值为 1 |
| `P3:45` | 当前配置项为 P3，值为 45 |
| `P4:20` | 当前配置项为 P4，值为 20 |

实际数码管只有四位；当值小于 10 时，十位为空白。

## 9. 配置项详解

### 9.1 P1：方向反向

| 项 | 内容 |
| --- | --- |
| 显示 | `P1` |
| 范围 | `0..1` |
| 默认值 | `0` |
| 修改方式 | EC11 顺时针设为 1，逆时针设为 0 |

含义：

- `0`：方向不反向。
- `1`：方向反向。

使用场景：

- 方向开关硬件接线和预期相反。
- 电机方向与遥控器显示的前进/后退相反。

### 9.2 P2：舵机反向

| 项 | 内容 |
| --- | --- |
| 显示 | `P2` |
| 范围 | `0..1` |
| 默认值 | `0` |
| 修改方式 | EC11 顺时针设为 1，逆时针设为 0 |

含义：

- `0`：舵机方向不反向。
- `1`：舵机方向反向。

使用场景：

- 转向电位器往左转时，实际舵机往右。
- 舵机安装方向或连杆结构和默认方向相反。

### 9.3 P3：舵机中位

| 项 | 内容 |
| --- | --- |
| 显示 | `P3` |
| 范围 | `20..70` |
| 默认值 | `45` |
| 修改方式 | EC11 旋转逐步增减 |

含义：

`P3` 用于调整舵机中位。默认 `45` 表示不偏移。数值变大或变小会让发给 receiver 的舵机角度整体偏移，用于校正车辆直行时的机械中位。

调试建议：

1. 先让转向电位器处于机械中间位置。
2. 观察前轮是否正中。
3. 如果前轮偏左或偏右，调整 `P3`。
4. 每次调整后观察实际舵机和前轮位置。

### 9.4 P4：舵机端点收缩

| 项 | 内容 |
| --- | --- |
| 显示 | `P4` |
| 范围 | `0..60` |
| 默认值 | `20` |
| 修改方式 | EC11 旋转逐步增减 |

含义：

`P4` 用于限制舵机最大左右行程，避免舵机或转向结构顶到机械端点。

实际限制规则：

```text
舵机角度下限 = P4
舵机角度上限 = 180 - P4
```

示例：

| P4 值 | 最终舵机角度范围 |
| ---: | --- |
| 0 | `0..180` |
| 20 | `20..160` |
| 30 | `30..150` |
| 60 | `60..120` |

P4 越大，舵机可用行程越小。

## 10. 推荐配置流程

首次装车或更换舵机/连杆后，建议按这个顺序调：

1. 进入配置模式。
2. 设置 `P2`，确认转向方向正确。
3. 设置 `P3`，让车辆在转向电位器中位时尽量直行。
4. 设置 `P4`，限制左右最大转角，避免顶死。
5. 设置 `P1`，确认前进/后退方向和显示一致。
6. 长按 EC11 中键约 3 秒保存并退出。
7. 重新上电或重新连接后复查配置是否保持。

如果只是在使用中临时调整速度、刹车、灯光或蜂鸣器，不需要进入配置模式。

## 11. 常见现象和处理

| 现象 | 可能原因 | 处理 |
| --- | --- | --- |
| 开机一直显示 `Cxxx` | 正在尝试保存频道 | 等待 receiver 上电并确认频道一致 |
| 显示 `Lxxx` | 当前频道收不到合法 ACK | 检查 receiver 电源/距离；需要时双击 EC11 中键扫频 |
| 显示 `E001` | 遥控器 nRF24 初始化失败 | 检查 nRF24 供电、焊接、SPI 和模块 |
| 正常界面冒号快闪 | 链路近期有失败或 ACK 缺失 | 拉近距离、检查天线/供电/遮挡 |
| 速度显示 `A0` | 当前速度为 100 | 正常显示 |
| 按刹车后第 1 位变 `-` | 当前处于刹车，速度位保持当前数值 | 正常行为 |
| 独立刹车松开后仍是原速度 | 独立刹车不清速度 | 正常行为 |
| 配置后断电未保存 | 未长按 3 秒保存退出 | 重新进入配置并保存 |

## 12. 与核心逻辑文档的关系

两份根目录文档分工如下：

| 文档 | 重点 |
| --- | --- |
| `遥控器操作与界面配置说明.md` | 遥控器如何使用、界面怎么看、配置模式怎么调 |
| `遥控器与接收机核心逻辑说明.md` | 系统架构、控制包、RF 参数、PWM 参数、receiver 输出和安全态 |

后续维护时，界面文案、EC11 操作、配置项说明优先更新本文档；协议字段、PWM 频率、控制周期、receiver 输出映射优先更新核心逻辑文档。
