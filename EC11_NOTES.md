# EC11 编码器经验

本文记录本项目 controller 侧 EC11 旋转编码器的排查和最终稳定做法。

## 引脚定义

当前项目与旧遥控器 PCB 一致：

- `EC11_A = P1.1`
- `EC11_B = P1.0`
- `EC11_SW = P5.4`

对应文件：

- `controller/src/board_pins.h`
- `legacy/old-prj/.../main.c`

因此，这次问题的根因不是引脚映射写错。

## 已验证过的关键结论

### 1. 50ms 主循环扫描完全不够

早期版本里，EC11 只在主循环中低频扫描。  
这种做法在慢速旋转时就会漏采，更不用说快速旋转。

经验结论：

- EC11 A/B 不能依赖粗颗粒主循环采样
- 必须独立、固定周期采样

### 2. A/B 采样应放到定时器 ISR

当前稳定方案：

- 用 `Timer0` 固定周期中断采样 EC11 A/B
- 当前采样周期约为 `0.5ms`
- ISR 里只做最小工作：读 A/B、调用 `drv_ec11_scan_delta_small()`、累计结果
- 主循环只消费累计 delta，不直接参与 A/B 解码

这个架构能把显示刷新和无线发包对编码器采样的干扰降到最低。

### 3. 高速旋转时，delta 累积器不能用 `s8`

曾经为了省体积，把编码器累计量压成 `stc8h_s8`。  
结果是在非常快速旋转时，累计值可能超过 `127` 后回绕成负数，出现：

- 正向快速旋转后突然减小
- 数值从较大值跳到个位数
- 高速时偶发反向减几

最终修复：

- `speed_encoder_delta_accum` 必须使用 `stc8h_s16`

这是一个真实 bug，不是手感问题。

### 4. 显示体验问题不等于 TM1637 本身有问题

之前看起来像“屏幕刷新太慢”，但本质上是：

- 速度值只在粗节拍主循环里被消费
- 所以显示只能看到合并后的大跳变

当前处理方式：

- 无线发包维持较低频率
- UI 更新频率单独提高

这样可以在快转时看到更连续的数值变化。

## 与基础库 `drv_ec11` 的边界

这次有一部分问题属于基础库职责，已经在 `Stc8hBase` 修过：

- detent 相位记录
- 回到 detent 才提交一次增量
- full-detent 下对漏采中间相位的容忍

本项目不应继续在业务层绕开这些问题。

## 旧项目快转加速参数

旧项目并不是始终 `+1/-1`，而是按相邻有效步进的时间间隔做加速。

本项目当前最终保留两档：

- `<= 6ms`：步长 `10`
- `<= 15ms`：步长 `5`
- 更慢：步长 `1`

说明：

- 旧项目里更激进的 `<= 3ms -> 步长 20` 已在当前项目中去掉
- 原因是这档在当前硬件和新采样架构下过猛，手感不如保留两档稳定

参考位置：

- `legacy/old-prj/.../main.c`

## 当前项目中的加速实现原则

当前项目实现快转加速时，采用以下原则：

1. 参考旧项目验证过的快转阈值，不重新发明参数
2. 判定依据是“相邻有效 detent 提交之间的时间间隔”
3. 时间间隔在编码器事件路径上记录，而不是用主循环节拍估算
4. 只在正常控制模式启用加速
5. 配置模式禁用加速，避免参数调节时一下跳太多

## 当前已确认有效的组合

对本项目 controller 来说，当前比较稳定的组合是：

- `drv_ec11 small API`
- `steps_per_detent = 4`
- `Timer0` 约 `0.5ms` 固定采样
- `s16` 累积器
- 正常模式启用两档快转加速（`<= 6ms -> 10`，`<= 15ms -> 5`）
- 配置模式禁用快转加速

## 实践建议

后续如果再出现 EC11 手感异常，优先按这个顺序排查：

1. 看 A/B 原始电平是否真的跳变
2. 确认采样是否仍是独立定时器 ISR
3. 检查 delta 累积器是否发生回绕
4. 检查 UI 消费节拍是否过低
5. 最后再看是否需要微调快转加速阈值

不要一开始就怀疑：

- 引脚映射
- TM1637 显示器本身
- 旧项目参数完全失效

大多数问题都出在采样架构、累积器位宽和消费节拍上。
