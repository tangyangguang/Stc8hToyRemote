# 遥控器与接收机核心逻辑说明

本文是 ToyRemote 系统的核心逻辑说明，用于让读者不阅读源码也能理解遥控器 controller、接收机 receiver、无线数据包、配置项和安全策略之间的关系。

## 1. 系统概览

ToyRemote 由两块 STC8H1K08 固件组成：

- controller：读取遥控器输入，归约成控制状态，通过 nRF24L01 发送控制包，并显示连接、速度、方向、配置和电压信息。
- receiver：接收并校验控制包，按绑定 ID 过滤来源，然后驱动电机、舵机、灯、蜂鸣器和辅助 PWM，并通过 ACK payload 回传链路状态和接收端电压。

系统的核心原则是：controller 只发送已经归约好的业务控制值，receiver 不解释原始按键，只执行控制包里的字段。无线 payload 必须按字节手工打包，不直接发送 C struct。

```text
遥控器输入
  -> controller 归约和配置校准
  -> 11 字节 ToyRemote 控制 payload
  -> 32 字节 proto_rf_link DATA 包
  -> nRF24L01
  -> receiver 校验、绑定过滤
  -> 本地输出
  -> 15 字节 ACK 状态回传
```

## 2. Controller 操作逻辑

### 2.1 控制输入

当前正式固件中，controller 参与控制的输入包括 6 个数字按键/开关、1 个 EC11 旋转输入和 1 路转向 ADC。

| 输入 | 引脚/来源 | 逻辑 | 归约后的字段 |
| --- | --- | --- | --- |
| EC11 旋转 | P1.1 / P1.0 | 调整速度；正常模式支持快转加速 | `speed=0..100` |
| EC11 中键 | P5.4 | 普通模式按下时刹车并清速度；长按 5 秒进设置；丢联时双击扫频 | `brake=1` 且 `speed=0`；长按/双击是本机 UI 行为 |
| 方向键/方向开关 | P3.7 | 前进/后退，低电平有效 | `direction=0..1` |
| 灯键 | P3.6 | 灯开关，低电平有效 | `light=0..1` |
| 蜂鸣器键 | P3.5 | 蜂鸣器开关，低电平有效 | `buzzer=0..1` |
| 独立刹车键 | P3.0 | 刹车，低电平有效；不清速度 | `brake=0..1` |
| Fn 键 | P3.4 | 请求/显示电压，低电平有效 | `request_voltage=0..1` |
| 转向电位器 | ADC11 / P3.3 | 转向输入；先映射到角度，再应用配置校准 | `steering_angle=0..180` |
| AUX PWM | 无当前输入 | 协议和 receiver 输出保留，但 controller 当前不控制 | `aux_pwm=0` |

只按按钮/开关数量计算，当前有 6 个：EC11 中键、方向、灯、蜂鸣器、独立刹车、Fn。按控制输入通道计算，则还包括 EC11 旋转和转向 ADC。

### 2.2 速度

EC11 旋转改变速度：

- 取值范围：`0..100`。
- 慢转：每个有效 detent 改变 1。
- 快转：相邻有效 detent 间隔不超过约 15ms 时，每步改变 5。
- 超出范围时饱和到 `0` 或 `100`。
- 配置模式禁用快转加速，每步只改变 1，避免配置值跳变。

速度只表示期望输出比例，不直接等于 PWM duty。receiver 会把 `speed=5..100` 映射到电机最低有效 duty 到 100% duty。

### 2.3 刹车

当前有两个刹车来源：

| 来源 | 行为 |
| --- | --- |
| EC11 中键按下 | `brake=1`，并立即把 `speed` 清 0 |
| 独立刹车键按下 | `brake=1`，但保留当前 `speed` |

两者最终都会让 receiver 执行刹车输出。区别是 EC11 中键更像“急停/清速度”，独立刹车键更像“按住刹车，松开后速度仍保持原设置”。

### 2.4 转向

转向输入来自 ADC11：

```text
ADC 原始值 0..1023
  -> 映射到 steering_angle 0..180
  -> 可选舵机反向
  -> 应用中位偏移
  -> 应用端点收缩
  -> 发包
```

默认中位配置为 `45`。发包前使用下面的逻辑调整：

```text
实际角度 = 原始角度 + steering_middle * 2 - 90
实际角度下限 = steering_reduce
实际角度上限 = 180 - steering_reduce
```

默认 `steering_middle=45` 时，中位偏移为 0。默认 `steering_reduce=20` 时，最终角度限制在 `20..160`，避免舵机打到机械端点。

### 2.5 灯、蜂鸣器和 Fn

- 灯键直接映射为 `light=1` 或 `0`。
- 蜂鸣器键直接映射为 `buzzer=1` 或 `0`。
- Fn 键当前不是普通输出开关，而是电压显示请求：`request_voltage=1` 时 receiver 才低频采样并通过 ACK 回传接收端电压。

controller 在显示电压时会在本机电压和 receiver 回传电压之间切换显示。

### 2.6 连接、丢链和扫频

controller 上电后优先使用保存频道：

```text
读取配置
显示 Cxxx
在保存频道发送控制包并等待合法 ACK
  成功 -> 进入正常控制界面
  连续失败超过阈值 -> 显示 Lxxx，锁定当前频道继续重试
```

进入 `Lxxx` 后，controller 不自动扫频。只有在 `Lxxx` 丢链界面双击 EC11 中键，才进入手动扫描：

```text
先扫描 16 个预设频道
再扫描 0..125 全部频道
找不到则重复这一整轮
找到后显示 Fxxx，保存频道，回到连接状态
```

16 个预设频道为：

```text
76, 72, 68, 64, 60, 56, 52, 48,
44, 40, 36, 32, 28, 24, 20, 16
```

第二段全频道扫描包含 `0..125` 的全部 126 个频道，不跳过已经在预设频道里出现过的频道。这样逻辑更简单，速度影响很小。

### 2.7 显示语义

| 显示 | 含义 |
| --- | --- |
| `C076` | 正在尝试频道 76 |
| `L076` | 频道 76 暂时连不上，锁定该频道重试 |
| `S076` | 正在扫描频道 76 |
| `F076` | 找到频道 76，短暂停留后进入控制界面 |
| `E001` | controller nRF24 初始化失败 |

正常控制界面：

| 显示元素 | 含义 |
| --- | --- |
| 第 1 位 `^` / `v` | 前进 / 后退 |
| 第 1 位 `-` | 刹车或安全控制状态 |
| 第 3-4 位 `00..99`、`A0` | 速度 `0..100`，其中 `A0` 表示 100 |
| 冒号灭 | 最近通信正常 |
| 冒号快闪 | 短时发送失败或 ACK 缺失 |

## 3. Controller 配置信息

controller 配置保存在 EEPROM。进入配置模式后先修改 RAM 草稿，只有长按保存退出时才写入 EEPROM；断电会放弃未保存的草稿。

进入和退出：

```text
正常模式长按 EC11 中键 5 秒 -> 进入配置模式
配置模式短按 EC11 中键 -> 切换配置项
配置模式旋转 EC11 -> 修改当前配置项
配置模式长按 EC11 中键 3 秒 -> 保存并退出
```

配置模式下，controller 仍会发包，但发包内容保持停止/刹车控制，避免配置时继续按原速度运行：

```text
speed = 0
brake = 1
light = 0
buzzer = 0
aux_pwm = 0
request_voltage = 0
```

配置项：

| 显示项 | 含义 | 范围 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| `P1` | 方向反向 | `0..1` | `0` | 反转 `direction` |
| `P2` | 舵机反向 | `0..1` | `0` | 反转 `steering_angle` |
| `P3` | 舵机中位 | `20..70` | `45` | 发包前参与中位偏移 |
| `P4` | 舵机端点收缩 | `0..60` | `20` | 限制最终舵机角度上下限 |

其他 controller 配置：

| 配置 | 默认值 | 说明 |
| --- | --- | --- |
| `tx_id` | `0x4A21` | controller 身份 ID；receiver 绑定后只接受匹配 ID |
| `last_channel` | `76` | 上次成功连接频道；扫频成功后保存 |

## 4. 无线数据包格式

### 4.1 链路层固定包

controller 发送固定 32 字节 nRF24 payload。前 9 字节是 `proto_rf_link` 头，后面是 ToyRemote 业务 payload。

| 字节 | 字段 | 值/范围 | 说明 |
| ---: | --- | --- | --- |
| 0 | magic | `0xA5` | 链路包标识 |
| 1 | link version | `0x01` | 链路协议版本 |
| 2 | packet type | `3` | DATA |
| 3 | seq_tx | `0..255` 循环 | controller 发送序号 |
| 4 | seq_rx | `0..255` 或 0 | 最近接收序号；当前固定路径可为 0 |
| 5 | flags | `1` | 要求 ACK |
| 6 | local_id | `1` | controller ID |
| 7 | peer_id | `2` | receiver ID |
| 8 | payload_len | `11` | ToyRemote 控制 payload 长度 |
| 9..19 | control payload | 见下表 | 业务控制字段 |
| 20..31 | padding | `0` | 未使用尾部补 0 |

### 4.2 控制 payload

控制 payload 固定 11 字节，从 32 字节包的 byte 9 开始。

| 包内字节 | payload 字节 | 字段 | 范围 | 说明 |
| ---: | ---: | --- | --- | --- |
| 9 | 0 | version | `1` | ToyRemote 业务协议版本 |
| 10 | 1 | direction | `0..1` | `0` 前进，`1` 后退 |
| 11 | 2 | speed | `0..100` | 速度百分比逻辑值 |
| 12 | 3 | brake | `0..1` | `1` 表示刹车 |
| 13 | 4 | steering_angle | `0..180` | 舵机角度逻辑值 |
| 14 | 5 | light | `0..1` | 灯 |
| 15 | 6 | buzzer | `0..1` | 蜂鸣器 |
| 16 | 7 | aux_pwm | `0..100` | 辅助 PWM；当前正常为 0 |
| 17 | 8 | request_voltage | `0..1` | 请求 receiver 电压 |
| 18 | 9 | tx_id low | `0x00..0xFF` | controller ID 低字节 |
| 19 | 10 | tx_id high | `0x00..0xFF` | controller ID 高字节 |

`tx_id=0` 无效。默认 `tx_id=0x4A21` 时，byte 18 为 `0x21`，byte 19 为 `0x4A`。

### 4.3 状态 ACK payload

receiver 通过 nRF24 ACK payload 回传状态。ACK payload 固定 15 字节：9 字节 `proto_rf_link` 头加 6 字节 ToyRemote 状态 payload。

状态 payload：

| payload 字节 | 字段 | 范围 | 说明 |
| ---: | --- | --- | --- |
| 0 | version | `1` | ToyRemote 业务协议版本 |
| 1 | link_state | `0..3` | `0` idle，`1` connecting，`2` connected，`3` lost |
| 2 | voltage_int | `0..99` | 电压整数部分 |
| 3 | voltage_dec | `0..99` | 电压小数部分，百分之一伏 |
| 4 | tx_id low | `0..255` | receiver 已绑定 controller ID 低字节 |
| 5 | tx_id high | `0..255` | receiver 已绑定 controller ID 高字节 |

电压示例：7.42V 回传为 `voltage_int=7`、`voltage_dec=42`。

## 5. Receiver 核心逻辑

### 5.1 启动

receiver 启动流程：

```text
初始化 GPIO、PWM、ADC、SPI、nRF24
读取 EEPROM 配置
检查 P30+P31 上电清绑定
输出进入安全态
初始化 PRX 接收
预装 ACK 状态 payload
进入主循环
```

如果 nRF24 初始化失败，receiver 会保持安全态，并通过 LED 进入射频错误状态。

### 5.2 校验和绑定

receiver 收到包后，先校验链路层，再校验 ToyRemote 控制 payload。

必须满足：

- 链路 magic、version、packet type、源/目的 ID 正确。
- payload 长度为 11。
- ToyRemote `version=1`。
- `direction/speed/brake/steering_angle/light/buzzer/aux_pwm/request_voltage` 都在合法范围内。
- `tx_id != 0`。

绑定规则：

```text
未绑定:
  收到第一个合法 tx_id -> 保存为 bound_tx_id，并应用本次输出

已绑定:
  tx_id == bound_tx_id -> 应用输出
  tx_id != bound_tx_id -> 丢弃，不改变输出
```

P30+P31 上电同时按下会清除 receiver 绑定。默认构建关闭运行期频道维护键，只保留上电清绑定。

### 5.3 输出动作

receiver 不关心“哪个按键被按下”，只关心收到的业务字段。

| 接收字段 | 本地动作 |
| --- | --- |
| `direction + speed + brake` | 控制 AT8236 IN1/IN2 PWM |
| `steering_angle` | 控制 P1.0 / PWM1P 舵机 |
| `light` | 控制 P3.5 灯输出 |
| `buzzer` | 控制 P3.6 蜂鸣器输出 |
| `aux_pwm` | 控制 P5.4 / PWM6_2 辅助 PWM |
| `request_voltage` | 触发 receiver 电压采样和 ACK 回传 |
| `tx_id` | 用于绑定和来源过滤 |

输出引脚：

| 功能 | 引脚/通道 | 逻辑 |
| --- | --- | --- |
| AT8236 IN1 | P3.3 / PWM7_2 | 前进 PWM |
| AT8236 IN2 | P3.4 / PWM8_2 | 后退 PWM |
| 舵机 | P1.0 / PWM1P | 50Hz 舵机 PWM |
| 辅助 PWM | P5.4 / PWM6_2 | 与 AT8236 共用 PWM-B 频率 |
| 灯 | P3.5 | 低电平亮 |
| 蜂鸣器 | P3.6 | 低电平响 |
| LED | P3.7 | 高电平亮，用于状态指示 |
| 电池电压 | ADC1 / P1.1 | 按请求采样并回传 |

### 5.4 电机和刹车

电机速度映射：

- `speed < 5`：前进和后退 duty 都为 0。
- `speed=5..100`：从最低有效 duty 线性映射到 100% duty。
- 默认最低有效 duty 为 20%，由 `APP_OUTPUT_MOTOR_MIN_DUTY_PERCENT=20u` 控制。
- AT8236 和辅助 PWM 使用 PWM-B，默认约 20.03kHz。

方向和刹车：

| 条件 | IN1 前进 PWM | IN2 后退 PWM |
| --- | --- | --- |
| `brake=1` | 100% | 100% |
| `brake=0, direction=0, speed<5` | 0 | 0 |
| `brake=0, direction=0, speed>=5` | 速度映射 duty | 0 |
| `brake=0, direction=1, speed<5` | 0 | 0 |
| `brake=0, direction=1, speed>=5` | 0 | 速度映射 duty |

刹车和安全态不同：刹车时 IN1/IN2 同时输出 100% duty；安全态下两路 duty 都是 0。

### 5.5 LED 状态

| 状态 | LED 表现 |
| --- | --- |
| 上电启动 | 快闪 3 次 |
| 射频初始化失败 | 双闪后长停顿 |
| 未绑定等待 | 单短闪后长停顿 |
| 已绑定等待或掉线 | 慢闪 |
| 已连接 | 常亮 |
| 清除绑定 | 快闪 6 次后进入未绑定等待 |

## 6. 安全态和异常处理

receiver 安全态用于保证失控时输出明确、可预测。

触发条件：

- 接收超时。
- 射频初始化失败。
- 持续收不到合法控制包。
- 本机频道维护切换后等待重新连接。

安全态动作：

```text
AT8236 前进 PWM = 0
AT8236 后退 PWM = 0
辅助 PWM = 0
灯关闭
蜂鸣器关闭
舵机回中
```

controller 侧丢链后不会继续假设 receiver 已执行最新控制。它会显示 `Lxxx` 并继续在当前频道重试，等待链路恢复或人工双击进入扫频。

## 7. 编译期和持久化配置

### 7.1 Controller 配置

| 配置 | 默认值 | 类型 | 说明 |
| --- | --- | --- | --- |
| `APP_TX_ID` | `0x4A21u` | 编译期默认值 / EEPROM 保存值 | controller 身份 ID |
| `APP_DEFAULT_RF_CHANNEL` | `76u` | 编译期默认值 / EEPROM 保存值 | 默认频道 |
| `APP_CONFIG_FLAG_DIRECTION_REVERSE` | bit `0x02` | EEPROM flags | 方向反向 |
| `APP_CONFIG_FLAG_STEERING_REVERSE` | bit `0x01` | EEPROM flags | 舵机反向 |
| `APP_CONFIG_DEFAULT_STEERING_MIDDLE` | `45u` | EEPROM 默认值 | 舵机中位 |
| `APP_CONFIG_DEFAULT_STEERING_REDUCE` | `20u` | EEPROM 默认值 | 舵机端点收缩 |

### 7.2 Receiver 配置

| 配置 | 默认值 | 说明 |
| --- | --- | --- |
| 默认 RF 频道 | `76u` | 默认构建运行时使用固定默认频道 |
| `bound_tx_id` | `0` | `0` 表示未绑定 |
| `APP_RECEIVER_ENABLE_CLEAR_BINDING_BUTTONS` | `1` | 默认启用 P30+P31 上电清绑定 |
| `APP_RECEIVER_ENABLE_CHANNEL_BUTTONS` | `0` | 默认关闭 P30/P31 运行期频道维护键 |

### 7.3 RF 配置

| 项 | 值 |
| --- | --- |
| 地址 | `TOYR1` |
| 默认频道 | `76` |
| 速率 | 250kbps |
| 发射功率 | 0dBm |
| Auto ACK | 开 |
| ACK payload | 开 |
| 控制包长度 | 32 字节 |
| 状态 ACK 长度 | 15 字节 |
| SETUP_RETR | ARD 1000us，ARC 15 |

### 7.4 输出配置

| 配置 | 默认值 | 说明 |
| --- | --- | --- |
| `APP_OUTPUT_FAST_PWM_PRESCALER` | `5u` | PWM-B 预分频 |
| `APP_OUTPUT_FAST_PWM_PERIOD` | `91u` | PWM-B 周期，11.0592MHz 下约 20.03kHz |
| `APP_OUTPUT_MOTOR_MIN_DUTY_PERCENT` | `20u` | 电机最低有效占空比百分比 |
| 舵机 PWM | 50Hz | P1.0 / PWM1P |

## 8. 取值范围汇总

| 字段/配置 | 范围 | 默认值/当前值 | 说明 |
| --- | --- | --- | --- |
| `direction` | `0..1` | `0` | `0` 前进，`1` 后退 |
| `speed` | `0..100` | `0` | EC11 旋转调整 |
| `brake` | `0..1` | `0` | EC11 中键或独立刹车键 |
| `steering_angle` | `0..180` | `90` 安全中位 | 发包前会应用配置校准 |
| `light` | `0..1` | `0` | 灯 |
| `buzzer` | `0..1` | `0` | 蜂鸣器 |
| `aux_pwm` | `0..100` | `0` | 当前无 controller 输入源 |
| `request_voltage` | `0..1` | `0` | Fn 键 |
| `tx_id` | `1..65535` | `0x4A21` | `0` 无效 |
| RF channel | `0..125` | `76` | nRF24 频道 |
| `link_state` | `0..3` | - | ACK 状态 |
| `voltage_int` | `0..99` | - | 电压整数部分 |
| `voltage_dec` | `0..99` | - | 电压小数部分 |
| 舵机中位配置 | `20..70` | `45` | 配置项 `P3` |
| 舵机端点收缩 | `0..60` | `20` | 配置项 `P4` |

## 9. 当前需要硬件核对的点

- `aux_pwm` 在 receiver 有实际输出，但 controller 端当前没有任何输入控制它；如果实际硬件上有对应按钮/旋钮，需要补控制映射。
- EC11 中键和独立刹车键都会产生刹车，但 EC11 中键会清速度，独立刹车键不清速度；需要确认这是否符合实际操作习惯。
- Fn 键当前只用于请求/显示电压，不是普通输出开关。
- 方向、灯、蜂鸣器、独立刹车、Fn、EC11 中键都是低电平有效输入；需要和实际 PCB 上拉/释放态核对。
- P30+P31 上电清绑定默认开启；需要确认实物释放态不会悬空误清绑定。
- AT8236 在约 20.03kHz、最低 20% duty、双路 100% 刹车下的温升、电流峰值和低速起步需要以目标电池/电机实测确认。

## 10. 相关源码入口

| 内容 | 文件 |
| --- | --- |
| ToyRemote 业务协议字段 | `shared/toy_remote_protocol.h` |
| 预设频道池 | `shared/toy_remote_channels.h` |
| controller 输入读取 | `controller/src/app_input.c` |
| controller 发包和 UI 主逻辑 | `controller/src/main.c` |
| controller 配置结构 | `controller/src/app_config.h` |
| receiver 收包、绑定和安全态 | `receiver/src/main.c` |
| receiver 输出执行 | `receiver/src/app_outputs.c` |
| receiver PWM 计算 | `receiver/src/app_outputs_calc.h` |
| 硬件引脚映射 | `controller/src/board_pins.h`、`receiver/src/board_pins.h` |
